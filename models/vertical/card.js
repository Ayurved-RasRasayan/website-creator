    renderProductCard(p) {        const meta = AppData.getCategory(p.category);        const discount = p.comparePrice ? Math.round((1 - p.price / p.comparePrice) * 100) : 0;        const nprPrice = AppData.formatNPR(p.price);        const stars = AppData.renderStars(p.rating);        const imgUrl = `/images/products/${p.category}/${p.image}`;        const forms = p.forms || [];        const showSelector = p.showFormSelector !== false && forms.length > 1;        const showBadge = !showSelector && forms.length === 1;        let formSelectorHTML = '';        if (showBadge) {            formSelectorHTML = `<span class="px-1.5 sm:px-2 py-1 sm:py-1.5 border border-emerald-200 rounded-lg text-[10px] sm:text-xs bg-emerald-50 text-emerald-700">${forms[0]}</span>`;        } else if (showSelector) {            formSelectorHTML = `<select id="form-${p.id}" class="px-1.5 sm:px-2 py-1 sm:py-1.5 border border-emerald-200 rounded-lg text-[10px] sm:text-xs bg-white outline-none focus:border-emerald-500">${forms.map((f, i) => `<option value="${p.defaultForm || 'raw'}" ${i === 0 ? 'selected' : ''}>${f}</option>`).join('')}</select>`;        }        return `        <div class="product-card group bg-white rounded-xl sm:rounded-2xl border border-emerald-100 overflow-hidden cursor-pointer hover:shadow-lg transition-all duration-200" onclick="UI.openProductModal(${p.id})">            <div class="relative h-40 sm:h-48 md:h-56 bg-gradient-to-br from-emerald-50 to-emerald-100 overflow-hidden">                <img src="${imgUrl}" alt="${p.name}" class="product-image w-full h-full object-contain p-4" loading="lazy" onerror="this.style.display='none';this.parentElement.innerHTML='<div class=\'flex items-center justify-center h-full text-4xl sm:text-5xl\'>${p.emoji}</div>'">                <div class="absolute top-1.5 sm:top-2 left-1.5 sm:left-2">                    <span class="px-1.5 sm:px-2 py-0.5 rounded-full text-[8px] sm:text-[10px] font-medium border ${meta ? meta.bgClass : ''}">${meta ? meta.emoji : ''} ${meta ? meta.label : ''}</span>                </div>                ${discount ? `<span class="absolute top-1.5 sm:top-2 right-1.5 sm:right-2 px-1.5 sm:px-2 py-0.5 bg-red-500 text-white text-[8px] sm:text-xs font-bold rounded-full">${discount}% OFF</span>` : ''}                ${p.featured ? `<span class="absolute bottom-1.5 sm:bottom-2 right-1.5 sm:right-2 px-1.5 sm:px-2 py-0.5 bg-emerald-600 text-white text-[8px] sm:text-xs font-bold rounded-full">★ Featured</span>` : ''}            </div>            <div class="p-3 sm:p-4 flex flex-col items-center text-center">                <h3 class="font-semibold text-emerald-900 text-xs sm:text-base line-clamp-1 mb-1">${p.name}</h3>                <span class="text-[9px] sm:text-[10px] text-emerald-600/70 font-medium mb-1">${p.defaultWeight}</span>                <div class="flex items-center gap-1 mb-1.5">                    <div class="flex items-center gap-0.5">${stars}</div>                    <span class="text-[9px] sm:text-xs text-emerald-600/70">(${p.reviews})</span>                </div>                <div class="flex items-baseline gap-1.5 mb-1">                    <span id="price-${p.id}" class="text-sm sm:text-lg font-bold text-emerald-900">$${p.price.toFixed(2)}</span>                    <span id="compare-${p.id}" class="text-[10px] sm:text-sm text-emerald-600/50 line-through ${p.comparePrice ? '' : 'hidden'}">${p.comparePrice ? '$' + p.comparePrice.toFixed(2) : ''}</span>                </div>                <div id="npr-${p.id}" class="text-[9px] sm:text-xs text-emerald-700 font-medium mb-2">${nprPrice}</div>                <div class="w-full flex flex-col gap-1.5" onclick="event.stopPropagation()">                    <select id="weight-${p.id}" onchange="UI.updateCardPrice(${p.id})" class="w-full px-2 py-1.5 border border-emerald-200 rounded-lg text-[10px] sm:text-xs bg-white outline-none focus:border-emerald-500">                        ${p.weights.map(w => `<option value="${w}" ${w === p.defaultWeight ? 'selected' : ''}>${w}</option>`).join('')}                    </select>                    ${formSelectorHTML}                    <button onclick="UI.quickAdd(${p.id}, event)" class="w-full px-3 py-1.5 bg-emerald-600 text-white text-[10px] sm:text-xs font-semibold rounded-lg hover:bg-emerald-700 transition-colors flex items-center justify-center gap-1 active:scale-95">                        <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/></svg>                        <span>Add to Cart</span>                    </button>                </div>            </div>        </div>`;    },